spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/services
  profiles:
    active: native
  security:
    user:
      password: admin


# Порт по которому можно будет обратится к config-service
server:
  port: 8001